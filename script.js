javascript // Add event listener to the document document.addEventListener("DOMContentLoaded", function() { console.log("Epic Tech AI is online!"); }); // Add animation to the header const header = document.querySelector("header"); header.addEventListener("mouseover", function() { header.style.transform = "scale(1.1)"; }); header.addEventListener("mouseout", function() { header.style.transform = "scale(1)"; }); // Add scroll animation to the sections const sections = document.querySelectorAll("section"); window.addEventListener("scroll", function() { sections.forEach(function(section) { if (section.offsetTop < window.scrollY + window.innerHeight / 2) { section.classList.add("animate"); } else { section.classList.remove("animate"); } }); });
